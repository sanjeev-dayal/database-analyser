# Backend dependencies (Python)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pandas>=2.2.0
duckdb>=1.1.0
openai>=1.50.0
python-dotenv>=1.0.0
pydantic>=2.8.0
python-multipart>=0.0.9

# Frontend dependencies are managed in db-analyser/package.json
# Install them with: npm install --prefix db-analyser
